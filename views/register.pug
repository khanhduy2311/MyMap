doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title Tạo tài khoản mới | MindTree
    link(rel="stylesheet", href="/css/style-register.css")
  body.register-page
    .video-bg
      video#bg-video(autoplay muted loop playsinline)
        source(src="/videos/galaxy.webm", type="video/webm")
        source(src="/videos/galaxy.mp4", type="video/mp4")
        img(src="/images/poster-galaxy.jpg", alt="background")

      .video-overlay
    .register-wrapper
      canvas#bokeh-canvas
      .register-card
        h1.logo MindTree
        p.subtitle Tạo tài khoản mới — nhanh chóng và an toàn.

        if error_msg && error_msg.length > 0
          .alert.alert-danger= error_msg

        form.register-form(action="/register", method="POST", novalidate)
          input(type="email", name="email", placeholder="Email", required)
          input(type="text", name="username", placeholder="Tên tài khoản", required)
          input(type="password", name="password", placeholder="Mật khẩu mới", required)
          button(type="submit", class="btn-register") Đăng ký

        .divider
        a(href="/login", class="login-link") Bạn đã có tài khoản? Đăng nhập
    script(src="/js/bokeh-register.js")
    script.
      (function(){ var v=document.getElementById('bg-video'); if(v && v.paused){ v.play().catch(function(){}); } })();
