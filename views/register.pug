doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title Tạo tài khoản mới | MindMap
    link(rel="stylesheet", href="/css/style-register.css")

  body.register-page
    //- 1. Đưa logo ra ngoài và đặt nó lên trên form
    h1.logo MindTree

    //- 2. Form đăng ký nằm trong container riêng
    .register-container
      h2 Tạo tài khoản mới
      p.subtitle Nhanh chóng và dễ dàng.
      
      hr.divider

      if error_msg && error_msg.length > 0
        .alert.alert-danger= error_msg

      form.register-form(action="/register", method="POST")
        div
          input(type="email", name="email", placeholder="Email", required)
        div
          input(type="text", name="username", placeholder="Tên tài khoản", required)
        div
          input(type="password", name="password", placeholder="Mật khẩu mới", required)
        
        div
          button(type="submit", class="btn-register") Đăng ký

      hr.divider

      a(href="/login", class="login-link") Bạn đã có tài khoản ư?