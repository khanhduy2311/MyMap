extends ../views/layouts/defaultHome.pug

block styles
  link(rel="stylesheet" href="/css/style-about.css")


block main
  .cinematic-bg
    .bg-layer
    .floating-particles
      - for (let i = 0; i < 15; i++)
        .particle(style=`width: ${Math.random() * 80 + 40}px; height: ${Math.random() * 80 + 40}px; top: ${Math.random() * 100}%; left: ${Math.random() * 100}%; animation-delay: -${Math.random() * 6}s;`)

  .header-container
    .nav-header
      .logo-section
        .innovation-badge Innovation & Technology
      .auth-buttons
        a.register-btn(href="/register") Đăng Ký
        a.login-btn(href="/login") Đăng Nhập

  .stars
    - for (let i = 0; i < 60; i++)
      .star(style=`width: ${Math.random() * 2 + 1}px; height: ${Math.random() * 2 + 1}px; top: ${Math.random() * 100}%; left: ${Math.random() * 100}%; animation-delay: -${Math.random() * 5}s;`)

  a.back-button(href="/") 
    i.fas.fa-arrow-left.me-2
    | Quay lại

  .page-wrapper
    .about-container
      section.about-hero
        .container
          .hero-content
            h1.hero-title Đội Ngũ Phát Triển
            p.hero-subtitle Những người đã biến ý tưởng thành hiện thực
      .divider-space(style="height: 70vh")
      section.team-section
        .container
          .team-grid
            .team-card
              img.team-photo(src="/uploads/avatars/dungct.jpg", alt="Dũng CT")
              h3.member-name Nguyễn Trung Dũng
              .member-id MSSV: 24520343
              .member-department Khoa Khoa học Máy tính
              .member-role Backend Developer & AI Specialist
              .member-tasks
                h4 Nhiệm vụ chính:
                ul
                  li Thiết kế kiến trúc hệ thống backend
                  li Phát triển API và xử lý logic nghiệp vụ
                  li Tích hợp và triển khai AI vào dự án
                  li Tối ưu hóa hiệu suất server và database

            .team-card
              img.team-photo(src="/uploads/avatars/duychan.jpg", alt="Duy Chan")
              h3.member-name Trần Đình Duy
              .member-id MSSV: 24520398
              .member-department Khoa Khoa học Máy tính
              .member-role Frontend Developer & UI/UX Designer
              .member-tasks
                h4 Nhiệm vụ chính:
                ul
                  li Thiết kế giao diện người dùng và trải nghiệm
                  li Phát triển animation và hiệu ứng cinematic
                  li Xây dựng responsive design cho mọi thiết bị
                  li Tối ưu hóa hiệu suất frontend và loading time

            .team-card
              img.team-photo(src="/uploads/avatars/beochan.jpg", alt="Béo Chan")
              h3.member-name Trương Đình Việt Dũng
              .member-id MSSV: 24520352
              .member-department Khoa Khoa học Máy tính
              .member-role Backend Developer & Database Architect
              .member-tasks
                h4 Nhiệm vụ chính:
                ul
                  li Thiết kế và quản lý cơ sở dữ liệu MongoDB
                  li Phát triển authentication và authorization
                  li Xây dựng real-time features và WebSocket
                  li Triển khai hệ thống và bảo mật ứng dụng
    
block scripts    
  script.
    document.addEventListener('DOMContentLoaded', function() {
      const heroContent = document.querySelector('.hero-content');
      const teamSection = document.querySelector('.team-section');

      // Sau 3.5s hero fade-out
      setTimeout(() => {
        if (heroContent) {
          heroContent.style.opacity = '0';
          heroContent.style.transform = 'translate(-50%, calc(-50% - 60px)) scale(0.9)';
          heroContent.style.filter = 'blur(20px)';
          heroContent.style.transition = 'all 1.2s ease-in-out';
        }

        // Sau khi hero biến mất xong, team fade-in
        setTimeout(() => {
          if (teamSection) {
            teamSection.style.opacity = '1';
            teamSection.style.visibility = 'visible';
            teamSection.style.transform = 'scale(1) translateY(0)';
          }
        }, 1200);
      }, 3500);

      // Clean up duplicate stars and particles
      const starsContainer = document.querySelector('.stars');
      while (starsContainer.children.length > 60) {
        starsContainer.removeChild(starsContainer.lastChild);
      }

      const particlesContainer = document.querySelector('.floating-particles');
      while (particlesContainer.children.length > 15) {
        particlesContainer.removeChild(particlesContainer.lastChild);
      }
    });