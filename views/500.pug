extends ../views/layouts/default.pug

block styles
  style.
    .error-container {
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      min-height: 70vh;
      text-align: center;
      padding: 30px;
      color: var(--text-primary);
    }
    .error-container h1 {
      font-size: 5rem;
      font-weight: 700;
      color: #e74c3c;
      margin: 0;
    }
    .error-container p {
      font-size: 1.25rem;
      margin-top: 10px;
    }
    .error-container .btn-home {
      margin-top: 25px;
      padding: 12px 25px;
      background-color: #3498db;
      color: white;
      text-decoration: none;
      border-radius: 8px;
      font-weight: 500;
      transition: background-color 0.2s ease;
    }
    .error-container .btn-home:hover {
      background-color: #2980b9;
    }
    .error-details {
      background: var(--bg-secondary);
      border: 1px solid var(--border-color);
      padding: 20px;
      margin-top: 30px;
      border-radius: 8px;
      max-width: 900px;
      width: 100%;
      text-align: left;
      font-family: 'Courier New', Courier, monospace;
      overflow-wrap: break-word;
      font-size: 0.9rem;
    }
    .error-details pre {
      white-space: pre-wrap;
      word-break: break-all;
    }

block main
  .error-container
    h1 500
    p Rất tiếc, đã có lỗi xảy ra ở phía máy chủ.
    p Chúng tôi đang kiểm tra vấn đề này.
    a.btn-home(href="/dashboard") Quay về trang chủ
    
    //- Khối này sẽ hiển thị chi tiết lỗi thật (lỗi gốc)
    //- Nó chỉ hiện khi bạn chạy ở chế độ development
    if process.env.NODE_ENV === 'development' && error
      .error-details
        h4 Chi tiết lỗi (Development Mode):
        pre
          code= error.message
        hr(style="margin: 15px 0;")
        pre
          code= error.stack