doctype html
html(lang="vi")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title Quên Mật Khẩu | MindTree
    link(rel="stylesheet", href="/css/style-login.css") 
  body.login-page
    .container
      .left-panel
        h1.logo MindTree
        h2.slogan Đừng lo lắng, chúng ta sẽ lấy lại tài khoản của bạn ngay.
      .right-panel
        .login-form
          if error_msg && error_msg.length > 0
            .alert.alert-danger= error_msg
          if success_msg && success_msg.length > 0
            .alert.alert-success= success_msg
            
          h4.mb-3(style="font-weight: 600;") Tìm tài khoản của bạn
          p.text-muted(style="font-size: 0.9rem; margin-top: -10px; margin-bottom: 20px;")
            | Vui lòng nhập email của bạn để tìm kiếm tài khoản.
          
          form(action="/forgot-password", method="POST")
            div
              input(type="email", name="email", placeholder="Email", required)
            div
              button(type="submit", class="btn-login") Gửi
        
          .divider
          a(href="/login", class="btn-register" style="background-color: #6c757d;") Hủy