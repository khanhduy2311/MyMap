doctype html
html(lang="vi")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title Quên Mật Khẩu | MindTree
    link(rel="stylesheet", href="/css/style-forgot.css")
  body.forgot-page
    // Video nền (FN1 - soft abstract light)
    .video-bg
      video#bg-video(autoplay muted loop playsinline)
        source(src="/videos/forgot.webm", type="video/webm")
        source(src="/videos/forgot.mp4", type="video/mp4")
        img(src="/images/poster-forgot.jpg", alt="background")

      .video-overlay

    // Wrapper & card ở giữa
    .forgot-wrapper
      .forgot-card
        h1.logo MindTree
        p.quote “Bạn không mất gì - chỉ đang lấy lại thứ vốn thuộc về bạn.”
        if error_msg && error_msg.length > 0
          .alert.alert-danger= error_msg
        if success_msg && success_msg.length > 0
          .alert.alert-success= success_msg

        form.forgot-form(action="/forgot-password", method="POST", novalidate)
          label(for="email") Email đăng ký tài khoản
          input#email(type="email", name="email", placeholder="Email của bạn" required)
          button(type="submit", class="btn-send") Gửi

        .small-note
          p Nếu email tồn tại trong hệ thống, bạn sẽ nhận được hướng dẫn tiếp theo.

    // ensure mobile autoplay
    script.
      (function(){ var v=document.getElementById('bg-video'); if(v && v.paused){ v.play().catch(function(){}); } })();
